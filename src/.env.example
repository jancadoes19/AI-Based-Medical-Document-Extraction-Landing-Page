
# ── Backend ───────────────────────────────────────────────────────────────────
# URL of the Flask backend (used by Next.js rewrites in next.config.ts)
# In Docker Compose: http://backend:8000
# Local development: http://localhost:8000
BACKEND_URL=http://localhost:8000

# ── Public env vars (exposed to browser) ─────────────────────────────────────
# Set to your public domain in production
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Auth ──────────────────────────────────────────────────────────────────────
# JWT secret must match the backend's SECRET_KEY
# NEXT_PUBLIC_* not needed — JWT is handled server-side by Flask

# ── Optional: Groq API (if calling from frontend directly) ───────────────────
# NEXT_PUBLIC_GROQ_API_KEY=gsk_...
